info:
  name: Add Book Request
  type: http

http:
  method: POST
  url: http://localhost:8000/books
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
