info:
  name: Checkout Cart Request
  type: http

http:
  method: POST
  url: http://localhost:8000/orders/checkout
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​