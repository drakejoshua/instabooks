info:
  name: Add To Cart Request
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8000/user/cart
  body:
    type: form-urlencoded
    data:
      - name: book_id
        value: dsksks
        disabled: true
      - name: quantity
        value: "2"
        disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
